const names = require('./names')
const sayHi = require('./utils')
const data = require('./alternative-flavours');
require('./mind-grenade');

// sayHi(names.john);
// sayHi(names.peter);

// console.log(data.names[0]);
// console.log(data.SinglePerson.name);